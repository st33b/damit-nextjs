import styles from './register_cta.module.scss';

const RegisterCTA = () => {

  return (
    <div className={styles.RegisterCTA}>
      REGISTER CTA (or others)
    </div>
  )
}

export default RegisterCTA;
